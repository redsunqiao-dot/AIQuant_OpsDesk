# LangGraph 编排核心
langgraph>=0.2.50
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# Charles 节点依赖（DeepAgents + 通义千问 + vendor 内子脚本）
deepagents>=0.0.10
dashscope>=1.20.0
openai>=1.0.0
jieba>=0.42.0
rank-bm25>=0.2.2
faiss-cpu>=1.7.4

# Zoe 节点依赖（行情）
xtquant
pandas>=2.0.0
numpy>=1.24.0

# 工具
python-dotenv>=1.0.0

# 定时调度（APScheduler 工作流自动触发）
apscheduler>=3.10.0
