# Charles 节点：通义千问 API（必填）
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Charles 节点：模型选择（可选，默认 qwen-plus）
CHARLES_MODEL=qwen-plus

# Trader 节点：miniQMT 路径与账户（实盘下单时必填）
QMT_PATH=
ACCOUNT_ID=

# Trader 节点：dry-run 模式（默认 1，不真实下单；改为 0 才会调用 miniQMT）
TRADER_DRY_RUN=1
